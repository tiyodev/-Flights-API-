const ErrorCode = {
  INVALID_DEPARTURE_AIRPORT: 'invalid.param.departure_airport',
  INVALID_ARRIVAL_AIRPORT: 'invalid.param.arrival_airport',
  INVALID_DEPARTURE_DATE: 'invalid.param.departure_date',
  INVALID_RETURN_DATE: 'invalid.param.return_date',
  INVALID_TRIP_TYPE: 'invalid.param.trip_tip',
  INVALID_DATES: 'invalid.dates',
};

export default ErrorCode;
